# 2026-08-21 工作日志

## CI/CD 配置（GitHub Actions）
- 用户代码托管在 GitHub，部署到宝塔面板服务器
- 新增 `.github/workflows/deploy.yml` — GitHub Actions 工作流
  - 触发: push 到 main/master 分支 + 手动 workflow_dispatch
  - 并发控制: concurrency group，同一时间只跑一个部署
  - 12 个 step: checkout → JDK17+Maven 构建后端 jar → Node18+npm 构建前端 dist → scp 上传 jar → rsync 上传 dist → SSH 重启 systemd 服务 → 健康检查（curl 8080）
  - 用的第三方 action: appleboy/scp-action、easingthemes/ssh-deploy、appleboy/ssh-action
- 新增 `deploy/cms-backend.service` — systemd 服务文件模板（Restart=always 自动拉起 + 日志输出到文件 + 环境变量配置 DB_PASSWORD/JWT_SECRET）
- 新增 `docs/deploy-cicd.md` — CI/CD 配置指南（SSH 密钥生成、systemd 安装、GitHub Secrets 4 个、常见问题）
- 更新 README.md 项目结构 + 生产部署段落引用 CI/CD
- YAML 语法验证通过（pyyaml safe_load: 1 job, 12 steps, 2 triggers）

## admin 密码修改 SQL
- 用户要改 admin 密码，生成了常用密码 BCrypt 哈希（admin123/admin/123456/Admin@123/admin888）
- 提供 SQL: `UPDATE sys_user SET password='哈希' WHERE username='admin'`
