# 2026-08-19 工作日志

## 宝塔面板部署
- 用户服务器用宝塔面板（无 Docker），产出 `docs/deploy-bt.md` 部署指南
- 方案: 宝塔软件商店装 MySQL8/Redis/Nginx/Java项目管理器(JK17) → phpMyAdmin 导入 schema+data → Java 项目跑 jar（端口 8080 + `--server.address=127.0.0.1` 只监听本机 + 9 个环境变量）→ 网站托管 dist + /api 反代
- 提供方式 B 兜底: Java 项目管理器不可用时用 systemd（沿用 deploy-manual.md）
- 本地打包完成: `backend/target/cms-backend-1.0.0.jar`(49M，含 WebConfig /v1) + `frontend/dist/`(11s 构建)
- 踩坑: vite build 清空 dist 时被 WorkBuddy 安全删除 shim 拦截（trashItem/rmSync 报错），`rm -rf dist` 后重新 build 解决
- 打包工具链: IDEA 内置 Maven `D:\IDEA2025\IntelliJ IDEA 2025.2\plugins\maven\lib\maven3\bin\mvn.cmd` + JAVA_HOME=`D:\jdk17`（本机无独立 Maven，离线 -o 会缺 plexus 插件依赖，需联网）

## 去掉 Docker 配置
- 用户不用 Docker，删除 4 个文件: `docker-compose.yml`、`backend/Dockerfile`、`frontend/Dockerfile`、`frontend/nginx.conf`
- 改造 `application-prod.yml`: 所有 `${DB_HOST}` 等环境变量加默认值（127.0.0.1/3306/cms_db/root/123456），不再"少一个就启动失败"，仍支持环境变量覆盖
- 更新 `README.md`: 项目结构去掉 docker-compose.yml，快速开始去掉 Docker Compose 方式改为直接本地开发 + jar 部署，新增生产部署段落引用 deploy-bt.md 和 deploy-manual.md，环境变量表 DB_PASSWORD 默认值改 123456
- 更新 deploy-bt.md / deploy-manual.md: "少一个都启动失败""没有默认值"等描述改为"已有默认值，建议至少改 DB_PASSWORD 和 JWT_SECRET"
